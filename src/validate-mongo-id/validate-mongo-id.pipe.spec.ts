import { ValidateMongoIdPipe } from './validate-mongo-id.pipe';

describe('ValidateMongoIdPipe', () => {
  it('should be defined', () => {
    expect(new ValidateMongoIdPipe()).toBeDefined();
  });
});
